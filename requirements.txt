# Production dependencies for Vercel deployment
# Lightweight version without heavy ML libraries
# Optimized for serverless deployment

# Core
python-dotenv>=1.0.0

# Web Framework
Flask>=3.0.0
Flask-CORS>=4.0.0

# Data handling - using binary wheels for faster installation
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# Data acquisition
akshare>=1.12.0

# Machine Learning
scikit-learn>=1.3.0

# Database
sqlalchemy>=2.0.0

# Utilities
python-dateutil>=2.8.2
pytz>=2023.3

# Note: Heavy ML libraries (PyTorch, TensorFlow, Prophet) and desktop GUI libraries
# (matplotlib, customtkinter) moved to requirements-dev.txt for local development.
# This production file contains only the lightweight dependencies needed for web deployment.
