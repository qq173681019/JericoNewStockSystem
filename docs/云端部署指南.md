# 📱 云端部署指南 - 让手机随时随地访问

本指南将教您如何将 SIAPS 系统部署到云端，实现随时随地通过手机访问。

---

## 🎯 部署方案对比

| 方案 | 费用 | 难度 | 推荐度 | 适用场景 |
|------|------|------|--------|----------|
| **Railway** | 免费 500小时/月 | ⭐ 最简单 | ⭐⭐⭐⭐⭐ | 个人使用 |
| **Render** | 免费层 | ⭐ 简单 | ⭐⭐⭐⭐ | 个人使用 |
| **腾讯云轻量服务器** | ~24元/月 | ⭐⭐ 中等 | ⭐⭐⭐⭐ | 国内访问快 |
| **阿里云ECS** | ~30元/月 | ⭐⭐ 中等 | ⭐⭐⭐⭐ | 国内访问快 |
| **Heroku** | 免费层已取消 | ⭐⭐ 中等 | ⭐⭐ | 不推荐 |

---

## 🚀 方案一：Railway 部署（推荐，最简单）

### 为什么选择 Railway？
- ✅ **完全免费**：每月 500 小时免费额度
- ✅ **最简单**：从 GitHub 直接部署，无需配置
- ✅ **自动域名**：提供 HTTPS 域名
- ✅ **持续部署**：代码更新自动重新部署

### 步骤 1：准备项目

1. **创建 `Procfile` 文件**（告诉 Railway 如何启动应用）：

在项目根目录创建 `Procfile`（无扩展名）：
```
web: python run_web_ui.py --host 0.0.0.0 --port $PORT --no-browser
```

2. **创建 `runtime.txt` 文件**（指定 Python 版本）：
```
python-3.11.7
```

3. **修改 requirements.txt**（移除可能有问题的包）：

创建 `requirements-prod.txt` 用于部署：
```txt
python-dotenv>=1.0.0
akshare>=1.12.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
scikit-learn>=1.3.0
Flask>=3.0.0
Flask-CORS>=4.0.0
sqlalchemy>=2.0.0
python-dateutil>=2.8.2
pytz>=2023.3
matplotlib>=3.7.0
```

### 步骤 2：部署到 Railway

1. **访问 Railway**：
   - 打开 https://railway.app/
   - 点击 "Start a New Project"
   - 使用 GitHub 账号登录

2. **导入项目**：
   - 选择 "Deploy from GitHub repo"
   - 授权 Railway 访问您的 GitHub
   - 选择 `JericoNewStockSystem` 仓库

3. **配置环境变量**（可选）：
   - 在 Railway 项目设置中添加环境变量
   - `FLASK_ENV=production`
   - `PORT` 会自动设置

4. **部署**：
   - Railway 会自动检测 Python 项目
   - 自动安装依赖并启动应用
   - 等待 3-5 分钟完成部署

5. **获取访问地址**：
   - 部署完成后，点击 "Generate Domain"
   - 会得到类似 `your-app.railway.app` 的域名
   - 在手机浏览器输入此域名即可访问

### 步骤 3：修改代码支持 Railway

修改 `run_web_ui.py` 的 `main()` 函数，添加对 `PORT` 环境变量的支持：

```python
def main():
    """Main entry point"""
    # Parse command line arguments
    parser = argparse.ArgumentParser(description='SIAPS Web UI - 股票智能分析预测系统')
    parser.add_argument('--host', type=str, default='127.0.0.1',
                       help='主机地址 (默认: 127.0.0.1 仅本地访问, 使用 0.0.0.0 允许网络访问)')
    parser.add_argument('--port', type=int, default=None,
                       help='端口号 (默认: 5000)')
    parser.add_argument('--no-browser', action='store_true',
                       help='不自动打开浏览器')
    parser.add_argument('--mobile', action='store_true',
                       help='启用手机访问模式（等同于 --host 0.0.0.0）- 仅在可信任网络中使用')
    
    args = parser.parse_args()
    
    # If mobile mode is enabled, override host
    if args.mobile:
        args.host = '0.0.0.0'
    
    # Support Railway's PORT environment variable
    import os
    port = args.port or int(os.environ.get('PORT', 5000))
    host = args.host
    
    # ... rest of the code
```

---

## 🌐 方案二：Render 部署

### 步骤 1：准备配置文件

创建 `render.yaml`：
```yaml
services:
  - type: web
    name: siaps-web
    env: python
    buildCommand: pip install -r requirements-prod.txt
    startCommand: python run_web_ui.py --host 0.0.0.0 --port $PORT --no-browser
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
```

### 步骤 2：部署

1. 访问 https://render.com/
2. 使用 GitHub 登录
3. 点击 "New +" → "Web Service"
4. 连接您的 GitHub 仓库
5. 配置：
   - Name: `siaps-web`
   - Environment: `Python 3`
   - Build Command: `pip install -r requirements-prod.txt`
   - Start Command: `python run_web_ui.py --host 0.0.0.0 --port $PORT --no-browser`
6. 点击 "Create Web Service"

---

## 🇨🇳 方案三：腾讯云轻量应用服务器（国内访问快）

### 为什么选择腾讯云？
- ✅ **国内访问快**：服务器在中国大陆
- ✅ **价格便宜**：轻量服务器 2核2G 约 24元/月
- ✅ **稳定可靠**：大厂服务质量有保障

### 步骤 1：购买服务器

1. 访问 https://cloud.tencent.com/
2. 注册/登录账号
3. 进入 "轻量应用服务器"
4. 选择配置：
   - 地域：选择离您最近的
   - 镜像：Ubuntu 20.04 或 22.04
   - 套餐：2核2G内存（足够）
5. 购买并等待服务器创建

### 步骤 2：连接服务器

**Windows 用户**：
- 下载 PuTTY 或使用腾讯云网页控制台
- 输入服务器公网 IP 和密码登录

**Mac/Linux 用户**：
```bash
ssh root@your-server-ip
```

### 步骤 3：部署应用

在服务器上执行以下命令：

```bash
# 1. 更新系统
apt update && apt upgrade -y

# 2. 安装 Python 3.11
apt install -y python3.11 python3.11-venv python3-pip git

# 3. 克隆项目
cd /opt
git clone https://github.com/qq173681019/JericoNewStockSystem.git
cd JericoNewStockSystem

# 4. 创建虚拟环境
python3.11 -m venv .venv
source .venv/bin/activate

# 5. 安装依赖（使用精简版）
pip install Flask Flask-CORS python-dotenv requests pandas numpy sqlalchemy

# 6. 创建服务文件
cat > /etc/systemd/system/siaps.service << 'EOF'
[Unit]
Description=SIAPS Web Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/JericoNewStockSystem
Environment="PATH=/opt/JericoNewStockSystem/.venv/bin"
ExecStart=/opt/JericoNewStockSystem/.venv/bin/python run_web_ui.py --host 0.0.0.0 --port 5000 --no-browser
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 7. 启动服务
systemctl daemon-reload
systemctl enable siaps
systemctl start siaps

# 8. 检查状态
systemctl status siaps
```

### 步骤 4：配置防火墙

在腾讯云控制台：
1. 进入服务器详情
2. 点击 "防火墙"
3. 添加规则：
   - 应用类型：自定义
   - 协议：TCP
   - 端口：5000
   - 来源：0.0.0.0/0

### 步骤 5：访问

在手机浏览器输入：`http://your-server-ip:5000`

### （可选）配置域名和 HTTPS

1. **购买域名**：在腾讯云或阿里云购买域名
2. **域名解析**：添加 A 记录指向服务器 IP
3. **安装 Nginx**：
```bash
apt install -y nginx certbot python3-certbot-nginx

# 配置 Nginx
cat > /etc/nginx/sites-available/siaps << 'EOF'
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
EOF

ln -s /etc/nginx/sites-available/siaps /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx

# 申请 SSL 证书（免费）
certbot --nginx -d your-domain.com
```

现在可以通过 `https://your-domain.com` 访问了！

---

## 🔧 方案四：阿里云 ECS（类似腾讯云）

步骤与腾讯云类似：
1. 访问 https://www.aliyun.com/
2. 购买 ECS 服务器（选择轻量应用服务器更便宜）
3. 按照腾讯云的部署步骤操作
4. 配置安全组规则开放端口 5000

---

## 📝 维护和管理

### 查看日志
```bash
# 查看服务状态
systemctl status siaps

# 查看实时日志
journalctl -u siaps -f

# 查看最近 100 行日志
journalctl -u siaps -n 100
```

### 重启服务
```bash
systemctl restart siaps
```

### 更新代码
```bash
cd /opt/JericoNewStockSystem
git pull
systemctl restart siaps
```

### 修改端口
编辑服务文件：
```bash
nano /etc/systemd/system/siaps.service
# 修改 ExecStart 行中的端口号
systemctl daemon-reload
systemctl restart siaps
```

---

## 🔒 安全建议

### 1. 修改默认端口
不要使用 5000，改用其他端口如 8888：
```bash
ExecStart=/opt/JericoNewStockSystem/.venv/bin/python run_web_ui.py --host 0.0.0.0 --port 8888 --no-browser
```

### 2. 配置密码认证
在 `run_web_ui.py` 中添加简单的认证：
```python
from flask import request, Response
from functools import wraps

def check_auth(username, password):
    return username == 'admin' and password == 'your-strong-password'

def authenticate():
    return Response(
        'Please login', 401,
        {'WWW-Authenticate': 'Basic realm="Login Required"'})

def requires_auth(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        auth = request.authorization
        if not auth or not check_auth(auth.username, auth.password):
            return authenticate()
        return f(*args, **kwargs)
    return decorated

# 在路由上添加装饰器
@app.route('/')
@requires_auth
def index():
    return render_template('index.html')
```

### 3. 使用 HTTPS
始终使用 SSL 证书（Let's Encrypt 免费）

### 4. 限制访问 IP（可选）
如果只想自己使用，可以配置防火墙只允许特定 IP 访问。

---

## ❓ 常见问题

### Q1: Railway 免费额度用完了怎么办？
**A**: 每月 500 小时，如果不够：
- 升级到 Hobby 计划（$5/月）
- 或切换到腾讯云/阿里云

### Q2: 云服务器会自动停止吗？
**A**: 不会，云服务器是 7x24 小时运行的，除非您手动停止或到期。

### Q3: 手机访问很慢怎么办？
**A**: 
- 如果用国外服务器（Railway/Render），国内访问会慢
- 建议使用腾讯云/阿里云国内服务器

### Q4: 需要备案吗？
**A**:
- 使用 IP 访问：不需要
- 使用域名访问：需要备案（大约 2 周）
- Railway/Render：不需要备案

### Q5: 数据会丢失吗？
**A**: 
- Railway/Render: 容器重启后数据会丢失，需要使用外部数据库
- 云服务器：数据持久化，不会丢失

### Q6: 如何备份数据？
**A**:
```bash
# 备份数据库
cd /opt/JericoNewStockSystem
tar -czf backup-$(date +%Y%m%d).tar.gz data/ logs/

# 下载到本地（在本地电脑执行）
scp root@your-server-ip:/opt/JericoNewStockSystem/backup-*.tar.gz .
```

### Q7: 可以多人同时访问吗？
**A**: 可以，但需要注意：
- Flask 开发服务器建议 < 10 人同时使用
- 如需更多并发，使用 Gunicorn：
```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 run_web_ui:app
```

---

## 🎯 推荐方案总结

### 个人使用（免费）
**推荐 Railway**：
- 优点：完全免费，部署简单
- 缺点：国内访问可能较慢

### 国内访问（付费）
**推荐腾讯云轻量服务器**：
- 优点：国内访问快，价格便宜（24元/月）
- 缺点：需要一些 Linux 知识

### 长期稳定使用
**推荐阿里云 ECS + 域名 + HTTPS**：
- 优点：专业、稳定、安全
- 缺点：价格稍高（30-50元/月）

---

## 📞 技术支持

遇到问题？
1. 查看本文档的常见问题部分
2. 查看服务器日志 `journalctl -u siaps -f`
3. 在 GitHub 提交 Issue

---

## 🎉 部署成功！

恭喜您完成部署！现在可以随时随地用手机访问 SIAPS 系统了！

**记得保存您的访问地址：**
- Railway: `https://your-app.railway.app`
- 云服务器: `http://your-server-ip:5000` 或 `https://your-domain.com`

**首次访问建议：**
1. 在手机浏览器添加为书签
2. 在 iOS 上可以"添加到主屏幕"，像 App 一样使用
3. 设置强密码保护（如果公网访问）

享受随时随地的股票分析体验！📱📈
